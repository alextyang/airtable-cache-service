// Next.js also expects a concrete 404 page while building fallback responses for production.
// This service does not need custom 404 UI, so the page stays intentionally tiny.
export default function NotFoundPage() {
  return "Not Found";
}
